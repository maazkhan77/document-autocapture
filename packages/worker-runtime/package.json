{
  "name": "@document-autocapture/worker-runtime",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts src/worker.ts --format esm --out-dir dist --dts --splitting false",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@document-autocapture/core-engine": "workspace:*",
    "@document-autocapture/ml-tf-fallback": "workspace:*",
    "@tensorflow-models/coco-ssd": "^2.2.3",
    "@tensorflow/tfjs": "^4.22.0"
  }
}
